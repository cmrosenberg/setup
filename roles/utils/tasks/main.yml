- name: Install binutils, coreutils and findutils
  homebrew:
      name: "{{ item }}"
      state: present
  with_items:
      - binutils
      - coreutils
      - findutils
  when: ansible_distribution == "MacOSX"

- name: Install binutils, coreutils and findutils
  apt:
      name: "{{ item }}"
      state: present
  with_items:
      - binutils
      - coreutils
      - findutils
  when: ansible_distribution == "Ubuntu"
